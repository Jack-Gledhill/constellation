---
- name: Install krb5
  ansible.builtin.apt:
    name:
      - krb5-config
      - krb5-user
    state: present
    update_cache: true

- name: Configure Kerberos
  ansible.builtin.template:
    src: krb5.conf
    dest: /etc/krb5.conf
    owner: root
    group: root
    mode: '0644'